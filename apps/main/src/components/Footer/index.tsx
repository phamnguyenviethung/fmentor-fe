import { Box } from '@mui/material';
import React from 'react';

const Footer = () => {
  return (
    <Box
      sx={{
        bgColor: 'black',
        width: '100%',
        height: '100%',
      }}
    >
      Footer
    </Box>
  );
};

export default Footer;
